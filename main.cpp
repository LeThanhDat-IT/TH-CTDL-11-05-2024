#include "Header.h"

void main() {
	NODEPTR head;
	/*init(head);
	addFirst(head, 10);
	addFirst(head, 20);
	addFirst(head, 30);
	addFirst(head, 40);
	addFirst(head, 50);
	outputNode(head);
	if (findNode(head, 20) != NULL) {
		cout << endl;
		addBeforeMid(findNode(head, 20), 100);
		outputNode(head);
	}*/
	menu(head);
}